# `::reqmd_app`

A high level library useful for developing applications to work with the
requests defined in markdown documents.  This crate provides a way to customize
how requests are created from documents and an interface for sending them to
servers.
